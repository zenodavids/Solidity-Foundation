
// -the elements of a c programme are made up of characters and this characters can be seen as essays in english language.
// - a program in c assembles a bunch of characters called tokens. an example of tokens are;
// --comments - this gives info about the code.
/**/
//
// --#include - this tells the compiler to include the code below.
// --identifiers - this are often referred to as variable names.
// --int main() - this is a special identifier that tells us where the program will start executing from.
// --operators -  (+ * / - & etc) too are tokens
// --punctuation - (: ; { } etc) these are also tokens

//////////////////////////////////////////////////////
